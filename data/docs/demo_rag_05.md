# Demo RAG 05

The fixed retrieval evaluation uses Recall@k, Hit Rate@k, MRR, nDCG@k, and context precision. The system records retrieval latency per query and reports p50 and p95 latency. A fixed evaluation set makes comparisons repeatable across changes.
