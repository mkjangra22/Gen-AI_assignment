# Demo RAG 03

Each stored vector has metadata including document_id, source, chunk_index, and file_type. The retrieval endpoint can use source as a metadata filter, allowing queries to be restricted to a selected document or source.
